<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Name="Root"
             x:Class="AiTutor.Maui.Controls.LoadingOverlay"
             IsVisible="{Binding IsLoading, Source={x:Reference Root}}">
    <Grid BackgroundColor="#88FFFFFF">
        <Border Padding="26"
                BackgroundColor="#FFFFFF"
                Stroke="#DDE7E2"
                HorizontalOptions="Center"
                VerticalOptions="Center">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="22" />
            </Border.StrokeShape>
            <HorizontalStackLayout Spacing="16">
                <ActivityIndicator IsRunning="{Binding IsLoading, Source={x:Reference Root}}" />
                <Label Text="{Binding LoadingText, Source={x:Reference Root}}" FontSize="20" TextColor="#17324D" VerticalTextAlignment="Center" />
            </HorizontalStackLayout>
        </Border>
    </Grid>
</ContentView>
